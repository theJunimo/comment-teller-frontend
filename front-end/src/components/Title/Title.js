import React from 'react';
import styles from './Title.scss';

const Title = () => {
    return(
        <header>
            <h1>
                Comment Teller
            </h1>
        </header>
    )
}

export default Title;